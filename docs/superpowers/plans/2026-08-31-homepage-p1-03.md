# Homepage P1.03 Implementation Plan

> **For agentic workers:** Execute this plan task-by-task with RED → GREEN verification. Do not delegate this plan; the current session is the approved executor.

**Goal:** Complete the localized, claim-safe public homepage while preserving the approved hero composition and keeping `07 · Get started` as the final numbered section.

**Architecture:** Keep `src/app/(public)/page.tsx` as an ordered Server Component composition layer. Each homepage section remains focused under `src/components/site/home/`, reads catalog copy through `next-intl/server`, and uses semantic HTML plus the existing public design tokens. Static FAQ disclosure uses native `details`/`summary`, so no new client boundary or package is required.

**Tech Stack:** Next.js 16 App Router, React 19 Server Components, next-intl, TypeScript, Node test runner, existing public CSS.

---

## Section disposition

| Existing section           | Decision | Result                                                                                                                    |
| -------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------- |
| `HeroSection`              | Modify   | Preserve DOM/classes/visual composition; localize and replace unsupported timing/metric claims with factual capabilities. |
| `TrustBandSection`         | Modify   | Replace authority-logo implication with non-quantified orientation statements.                                            |
| `ServicesSection`          | Modify   | Complete Mainland/Free Zone/Offshore discovery comparison and supported setup/ongoing service coverage.                   |
| `EstimatorSection`         | Modify   | Keep estimator CTA; clearly label preview values illustrative/indicative with AED exclusions.                             |
| `FlowSection`              | Modify   | Present explore → estimate → business details → registration, including assigned-PRO handoff.                             |
| `WhyMandoobSection`        | Modify   | Replace guarantees/comparative assertions and remote art with factual one-company workflow capabilities.                  |
| `AnnotatedShowcaseSection` | Modify   | Label the preview illustrative, make its CTA a real link, remove savings claims, and fix desktop/reflow overflow.         |
| `CustomersSection`         | Retire   | Remove fabricated metrics, avatars, ratings, and testimonials.                                                            |
| `KnowledgeFaqSection`      | Create   | Add factual product principles, real Blog/Knowledge Base routes, and native accessible FAQ as section 06.                 |
| `FinalCtaSection`          | Modify   | Localize safe copy while preserving design, estimate/PRO routes, final position, and visible `07 · Get started`.          |

### Task 1: Homepage structure and final-section contract

**Files:**

- Create: `src/app/(public)/page.test.ts`
- Modify: `src/components/site/home/FinalCtaSection.test.ts`
- Modify: `src/app/(public)/page.tsx`

- [ ] Write source contracts asserting `HeroSection` is first, numbered sections are ordered, `FinalCtaSection` is last, and `07 · Get started` remains catalog-backed.
- [ ] Run the two tests and confirm RED because `KnowledgeFaqSection` and localized final copy do not exist.
- [ ] Add the ordered composition and minimal final-section localization hook.
- [ ] Re-run and confirm GREEN.

### Task 2: Claim and CTA contract

**Files:**

- Create: `src/components/site/home/homepage-content-contract.test.ts`
- Modify: `src/components/site/home/HeroSection.tsx`
- Modify: `src/components/site/home/TrustBandSection.tsx`
- Modify: `src/components/site/home/AnnotatedShowcaseSection.tsx`
- Modify: `src/components/site/home/WhyMandoobSection.tsx`

- [ ] Write a failing contract rejecting the PF1-AUDIT-006 metrics/testimonials/timing/savings strings, remote image URLs, hash-only links, and action-styled non-links.
- [ ] Run it and confirm RED against the accepted base.
- [ ] Replace unsupported claims with factual capability language, label synthetic states, and make apparent actions real links.
- [ ] Re-run and confirm GREEN.

### Task 3: Complete content groups

**Files:**

- Modify: `src/components/site/home/ServicesSection.tsx`
- Modify: `src/components/site/home/EstimatorSection.tsx`
- Modify: `src/components/site/home/FlowSection.tsx`
- Create: `src/components/site/home/KnowledgeFaqSection.tsx`
- Delete: `src/components/site/home/CustomersSection.tsx`

- [ ] Extend the failing structure/content contract to require setup comparison, six supported service categories, four journey steps, indicative estimate disclosure, real knowledge routes, and six FAQ topics.
- [ ] Run and confirm RED for the missing knowledge/FAQ and incomplete disclosures.
- [ ] Implement the minimum semantic section content, retaining section numbers 01–06 and supported product scope.
- [ ] Re-run and confirm GREEN.

### Task 4: English/Arabic catalog parity

**Files:**

- Modify: `src/messages/en.json`
- Modify: `src/messages/ar.json`
- Modify: `src/lib/i18n/messages.test.ts`
- Modify: all retained homepage section components

- [ ] Add a failing homepage catalog test for exact EN/AR leaf and ICU parity, Arabic-script content, and retained literal `07` numbering.
- [ ] Run and confirm RED because the `home` namespace is absent.
- [ ] Add natural UAE Arabic and English copy under identical `home` keys; convert Server Components to `getTranslations('home...')`.
- [ ] Re-run and confirm GREEN.

### Task 5: Responsive, theme, motion, and accessibility regression

**Files:**

- Create: `src/components/site/home/homepage-responsive-contract.test.ts`
- Modify: `src/app/(public)/public-theme.css`

- [ ] Write a failing CSS contract requiring logical positioning, bounded annotated chips/mock content, mobile table handling, 44px FAQ summary targets, visible focus, and reduced-motion coverage.
- [ ] Run and confirm RED against the accepted base.
- [ ] Add narrowly scoped homepage styles using existing tokens and logical properties; do not alter hero-specific CSS.
- [ ] Re-run and confirm GREEN.

### Task 6: Candidate verification and evidence

**Files:**

- Create: `Reports/launch-gate-evidence/2026-08-31/public-frontend-phase-1/p1-03-homepage/verification.md`
- Create: sanitized screenshots and `SHA256SUMS.txt` in the same evidence directory
- Modify after all gates pass: outer `docs/roadmap.md`
- Modify after all gates pass: outer `Reports/daily-work-report.md`

- [ ] Run the exact focused test discovery command, `npx tsc --noEmit`, `npm run lint`, `npm run format:check`, `git diff --check`, and `npm run build`.
- [ ] Record the protected hero/CSS diff against base SHA `77539f0e517e6fb7a4822b8633e1bfcbc1148fd7`.
- [ ] Run the candidate on port 3001 and verify EN/AR, LTR/RTL, light/dark, 1440×900, 390×844, 768×1024, 720px reflow, keyboard FAQ/CTA access, reduced motion, console, links, overflow, and axe Critical/Serious results.
- [ ] Capture the four required sanitized screenshots and SHA-256 hashes.
- [ ] Only after every criterion passes, mark P1.03 complete in the roadmap, append the daily report session, commit the final candidate, and stop before P1.04.
